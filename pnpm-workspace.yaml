packages:
  - "apps/*"
  - "packages/*"

# both of these fetch a native binary in a postinstall.
# esbuild is vitest's bundler, sharp is next's image optimiser.
allowBuilds:
  esbuild: true
  sharp: true
